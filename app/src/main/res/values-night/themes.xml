<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
        <!-- Base application theme. -->
        <style name="Theme.Aquario" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
            <!-- Primary brand color. -->
            <item name="accentColor">@color/orange </item>
            <item name="backgroundColor">@color/whitest</item>
            <item name="textColorDark">@color/black</item>
            <item name="textColorNormal">@color/black</item>
            <item name="textColorLight">@color/whitest</item>
            <item name="inputColor">@color/offwhite</item>
            <item name="colorPrimary">@color/orange</item>
            <item name="drawerColor">@color/offwhite</item>
            <item name="cardBackground">@color/offwhite</item>
            <item name="android:fontFamily">@font/nunito</item>

            <!-- Secondary brand color. -->
            <!-- Status bar color. -->
            <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
            <!-- Customize your theme here. -->
        </style>

        <style name="NoActionBarTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
            <item name="accentColor">@color/orange </item>
            <item name="backgroundColor">@color/whitest</item>
            <item name="textColorDark">@color/black</item>
            <item name="textColorNormal">@color/black</item>
            <item name="textColorLight">@color/whitest</item>
            <item name="inputColor">@color/offwhite</item>
            <item name="colorPrimary">@color/orange</item>
            <item name="drawerColor">@color/offwhite</item>
            <item name="cardBackground">@color/offwhite</item>
            <item name="android:fontFamily">@font/nunito</item>
        </style>
    </resources>
